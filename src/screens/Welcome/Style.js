import {StyleSheet} from 'react-native';
const WELCOME_STYLES = StyleSheet.create({
  INNER_CONTAINER: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
export default WELCOME_STYLES;
